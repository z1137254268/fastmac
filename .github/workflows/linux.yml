name: linux
on: 
  workflow_dispatch:
defaults:
  run:
    shell: bash

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    #- uses: fastai/workflows/ssh@master
    #  with:
    #    ssh_key: ${{ secrets.SSH_KEY }}
    - name: Setup tmate session
      uses: fastai/action-tmate@master
